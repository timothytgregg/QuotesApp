<%= form_for :collection_quote, url: '/collection_quotes' do |f| %>

  <p>
    <%= f.label :quote_id %><br>
    <%= f.text_area :quote_id %>
  </p>

  <p>
    <%= f.label :collection_id %><br>
    <%= f.text_area :collection_id %>
  </p>

  <p>
    <%= f.submit %>
  </p>

<% end %>

<%= link_to 'Back', collections_path %>
